---
"just-bash": patch
---

Parse process substitutions through the shell grammar and preserve adjacent word contexts, literal heredoc delimiters, arithmetic-leading bodies, and nested `LINENO` behavior.
